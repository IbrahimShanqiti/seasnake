# SeaSnake - Snake in Pure C

Snake in C alone, using SDL2. That's pretty much it. Very few (if any) frills.

Set up SDL and compile with make!

If you want each food eaten to increase your snake's length by ```n```, just use:
```./snake n```